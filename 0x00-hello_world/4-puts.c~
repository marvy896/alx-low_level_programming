#include <stdio.h>
#include <string.h>
/**
*initializing strings
*assign variables to the string
*writr to the std out
*return:0
*/

int main(void)
{
char alx[] = "Programming is like building a multilingual puzzle";
puts(alx);
return (0);
}
