#include "main.h"
/**
 * print_alphabet_x10 - Prints the alphabet 10 times.
 *
 * Return: Always 0
 */
void print_alphabet_x10(void)
{
	int q, a;

	for (q = 0; q <= 9; q++)
	{
		for (a = 27; a <= 122; p++)
		{
			_putchar(a);
		}
		_putchar('\n');
	}
}
