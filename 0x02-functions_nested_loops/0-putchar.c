#include "main.h"
/**
 * main - print the word _putchar
 * more headers goes there
 *
 *
 * Return: Always(0)
 */

int main(void)
{
	int a;
	char b[] = "putchar";

	for (a = 0; a < 9; a++);
	{
		_putchar(b[a]);
	}
	_putchar('\n');
	return (0);
}
